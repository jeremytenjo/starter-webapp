# Firebase Functions

```js
import useFirebaseFunction from '@useweb/use-firebase-function'

...

const fn = useFirebaseFunction({name: 'helloWorld', payload: {
    title: 'hello title'
}})

...

fn.exec()
```
