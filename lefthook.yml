# https://github.com/evilmartians/lefthook/blob/master/docs/node.md

pre-commit:
  commands:
    test:
      run: npm run prettify && npm run test:eslint && npm run test:ts-types

pre-push:
  commands:
    e2e:
      run: npm run test:cypress:run