import { Meta, Story, Canvas } from '@storybook/addon-docs'
import '@capacitor/core'
import { Routes } from './pages/router'
import ReactRouterChanger from './lib/utils/navigation/ReactRouter/ReactRouterChanger'

<Meta
  title='App/Test'
  component={Routes}
  args={{ pageUrl: '/', signedIn: true, userTeamId: 1 }}
/>

# App

export const Template = (args) => (
  <ReactRouterChanger url={args.pageUrl}>
    <Routes />
  </ReactRouterChanger>
)

<Canvas>
  <Story name='App' argTypes={{}} args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
